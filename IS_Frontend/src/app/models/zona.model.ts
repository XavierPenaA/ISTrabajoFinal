// src/app/models/zona.model.ts
export interface Zona {
  id: number;
  nombre: string;
  peligro: string; // Asumiendo que el campo 'peligro' es string
  // Agrega cualquier otro campo que tu modelo Zona en Django tenga y que necesites en el frontend
}
